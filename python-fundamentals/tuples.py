# TUPLES  --> immutable ( cannot be changes 'elements')

coordinates = (4, 5) # tuples
print(coordinates)
print(coordinates[1])

# tuples can also be used inside a LIst
# For example

sets = [(5,6), (7, 8), (9, 10)]
print(sets[1])